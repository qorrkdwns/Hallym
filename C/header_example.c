// 표준 입출력 함수 사용을 위한 헤더 파일
#include <stdio.h>  // printf, scanf, getchar, putchar 등

// 수학 관련 함수 사용을 위한 헤더 파일
#include <math.h>   // sqrt, pow, sin, cos, tan 등

// 문자열 처리 함수 사용을 위한 헤더 파일
#include <string.h> // strcpy, strcat, strlen, strcmp 등

// 유틸리티 함수 사용을 위한 헤더 파일
#include <stdlib.h> // atoi, malloc, free, rand, srand, exit 등

// 시간 및 날짜 관련 함수 사용을 위한 헤더 파일
#include <time.h>   // time, localtime, ctime, clock 등

// 정수 및 부동소수점 형식 관련 함수를 사용하기 위한 헤더 파일
#include <ctype.h>  // isdigit, isalpha, toupper, tolower 등

// 동적 메모리 관리 함수 사용을 위한 헤더 파일
#include <stddef.h> // NULL, size_t, ptrdiff_t 등

// 파일 입출력 관련 함수 사용을 위한 헤더 파일
#include <stdio.h>  // fopen, fclose, fread, fwrite, fseek 등

// 시스템 관련 함수 및 정의 사용을 위한 헤더 파일
#include <unistd.h> // sleep, getpid, fork 등 (유닉스 계열 시스템에서 사용)

// 일반적인 데이터 타입 정의 및 유틸리티 함수 사용을 위한 헤더 파일
#include <stdint.h> // fixed-width integer types (int32_t, uint64_t 등)
